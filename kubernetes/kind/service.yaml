apiVersion: v1
kind: Service
metadata:
  name: netflix-frontend-svc
  namespace: netflix-clone
spec:
  type: NodePort
  selector:
    app: netflix-frontend
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
